# ============================================================
# Runtime du dashboard de supervision (Streamlit Cloud)
# Dépendances strictement nécessaires à dashboard/supervision.py
# Pour l'environnement complet (entraînement ML, parsing), voir requirements-dev.txt
# ============================================================

streamlit>=1.37.0       # st.fragment(run_every=...) pour la page Live
plotly>=5.15.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
scipy>=1.11.0           # savgol_filter (pipeline)
scikit-learn>=1.3.0     # métriques (pipeline)
xgboost>=2.0.0          # importé par pipeline.corrosion_pipeline
matplotlib>=3.7.0       # importé par pipeline.corrosion_pipeline
joblib>=1.3.0
